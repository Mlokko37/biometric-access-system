system:
  name: Biometric-Based Student Access Control System
  environment: development

biometrics:
  fingerprint_threshold: 0.75
  face_threshold: 0.65
  max_attempts: 3

security:
  encryption: AES-256
  backup_authentication: true

logging:
  log_access_events: true
  retention_days: 365
